<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hanhan.demo.Dao.CourseDao">

    <!-- 课程添加功能 -->
    <insert id="add">
        INSERT course(id, course_no, course_name, course_prior_id, course_credit) VALUES (#{id}, #{courseNo}, #{courseName}, #{coursePriorId}, #{courseCredit})
    </insert>
</mapper>

